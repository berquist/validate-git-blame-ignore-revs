[project]
name = "validate-git-blame-ignore-revs"
dynamic = ["version"]
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eric Berquist", email = "eric.berquist@gmail.com" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
validate-git-blame-ignore-revs = "validate_git_blame_ignore_revs.__main__:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0", "versioningit>=3.1"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "dulwich>=0.24.1",
    "pygit2>=1.15.1",
    "pytest-cov>=6.2.1",
]

[tool.mypy]
strict = true

[tool.ruff]
line-length = 100

[tool.uv.sources]
dulwich = { git = "https://github.com/jelmer/dulwich.git", rev = "08f3bd541925f30e6eb0c4e773ffdaa14c202069" }

[tool.versioningit.format]
method = {module = "versioningit_override", value = "versioningit_format", module-dir = "tools"}

[tool.versioningit.template-fields.version-tuple]
pep440 = true

[tool.versioningit.vcs]
method = "git"
match = ["v*"]

[tool.versioningit.write]
file = "src/validate_git_blame_ignore_revs/_version.py"
